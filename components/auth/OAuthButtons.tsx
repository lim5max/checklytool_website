'use client'

interface OAuthButtonsProps {
  onError: (error: string) => void
}

export default function OAuthButtons({}: OAuthButtonsProps) {
  // OAuth авторизация временно отключена
  return null
}